network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      addresses:
        - {{ ansible_host }}/24
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
      routes:
        - to: default
          via: {{ gateway }}
